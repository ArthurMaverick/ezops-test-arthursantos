name: Realtime chat CI/CD
on:  
  push:
    branches: [main]
  pull_request:
    branches: [main]
    
jobs:

  # build:
  #   name: CI
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: main
        
  #   steps:
  #   - uses: actions/checkout@v2
  #   - name: logout
  #     run: docker logout 
    
  #   - name: Build The Docker Image
  #     run: docker build ./api --tag arthursantos2228/realtime-chat:latest

  #   - name: docker login 
  #     env:
  #       USER_SECURE: ${{secrets.USER_SECRET}}
  #       PASSWORD_SECURE: ${{secrets.PASS_SECRET}}
  #     run: |
  #       docker login -u $USER_SECURE -p $PASSWORD_SECURE 

  #   - name: Docker push 
  #     run: docker push arthursantos2228/realtime-chat:latest
  #  oi  05

  deploy-prod:
    name: CD
    runs-on: ubuntu-latest
    environment:
      name: deployprod
      url: 'http://13.66.205.253/messages'
    # needs: build

    steps:
    - name: deploy with ssh
      uses: appleboy/ssh-action@master
      with:
        host: ${{secrets.HOST}}
        username: ${{secrets.USERNAME}}
        key: ${{secrets.PRIVATE_KEY}}
        port: ${{secrets.PORT}}
        passphrase: ${{secrets.PASSWORD}} 
        debug: true
        script: |
          docker container rm realtime-chat \
          && docker image rm arthursantos2228/realtime-chat \
          && sudo docker-compose up -d
